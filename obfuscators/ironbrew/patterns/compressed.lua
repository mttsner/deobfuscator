local function decompress(b)
	local c,d,e="","",{}
	local f=256;
	local g={}
	for h=0,f-1 do 
		-- Performance 
	end;
	local i=1;
	local function k()
		--- Ignore for performance
	end;
	c=Char(k())
	e[1]=c;
	while i<#b do 
		-- Ignore this for performance reasons
	end;
	return table.concat(e)
end;
local ByteString=decompress(_StringExpr_);